export class Author {
    constructor(
        public id: number,
        public name: string,
        public bio: string,
        public nationality: string,
        public birthDate: string
    ) { }
}
